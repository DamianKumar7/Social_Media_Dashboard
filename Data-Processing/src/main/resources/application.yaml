server:
  port: 9081

spring:
  datasource:
    url: jdbc:postgresql://your-postgres-host:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Damian2000#
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5

# Configure JWT secret
jwt:
  secret: social-media-dashboard